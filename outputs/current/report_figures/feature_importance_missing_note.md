# Feature Importance Missing Note

Figure 7 (`fig7_lightgbm_feature_importance.png`) was not generated because the current `outputs/current/` artifacts do not contain a saved LightGBM 5D model object or a saved feature-importance table for the final `ml_risk_budget_enhancement` strategy.

The requested figure should not be fabricated from proxy metrics, and this task explicitly avoids retraining or reselecting models. To produce this figure in a future run, persist the fitted LightGBM model or export the top feature importances during the existing walk-forward training workflow.
